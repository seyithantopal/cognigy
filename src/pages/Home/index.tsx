import React, { FC } from 'react';
import Chat from '../../components/Chat';

const Home: FC = () => {
  return (
    <div>
      <h1>Home Page</h1>
      <Chat />
    </div>
  );
};

export default Home;
